Carpeta `app/models/`

Qué irá aquí:
- Modelos Pydantic para request/response y estructuras internas (`message_model.py`, `response_model.py`).
- Schemas y tipos compartidos usados por `api` y `services`.

Propósito:
- Centralizar esquemas de datos y validaciones con tipos claros.
