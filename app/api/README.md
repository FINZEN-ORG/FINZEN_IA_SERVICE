Carpeta `app/api/`

Qué irá aquí:
- Módulos que definen routers, dependencias y agrupaciones de endpoints.
- `dependencies.py`: dependencias compartidas (auth, DB session, request id, etc.)

Propósito:
- Mantener la capa HTTP (endpoints) separada de la lógica de negocio.
