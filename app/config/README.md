Carpeta `app/config/`

Qué irá aquí:
- `settings.py`: Definición de configuraciones con Pydantic (BaseSettings/pydantic-settings), lectura de `.env`.
- Otros módulos de configuración (p. ej. log config, constantes, esquemas de entorno).

Propósito:
- Centralizar la configuración de la aplicación y leer variables sensibles desde `.env`.
