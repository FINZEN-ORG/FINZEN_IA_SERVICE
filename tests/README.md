Carpeta `tests/`

Qué irá aquí:
- Pruebas unitarias y de integración.
- Tests para `services`, `clients` y `api`.

Propósito:
- Mantener cobertura y ejemplos de cómo mockear `clients` y probar `services`.
